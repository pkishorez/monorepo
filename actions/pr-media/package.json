{
  "name": "action-pr-media",
  "version": "0.0.0",
  "description": "GitHub action that attaches local images to a pull request in one comment that each run edits.",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc -p . && laymos lint",
    "test": "node --test 'tests/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
