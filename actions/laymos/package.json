{
  "name": "action-laymos",
  "version": "0.0.0",
  "description": "GitHub action that draws the Laymos Projects a pull request changed and keeps one comment with the pictures up to date.",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc -p . && laymos lint",
    "test": "node --test 'tests/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
