# laymos-test

Consumer-level integration coverage for ordinary Vitest tests and
metadata-enriched Laymos Tests.

```sh
pnpm --filter laymos-test test
```

The same files can be run through `laymos/node` to verify completed report
mapping without changing how the tests are authored.
