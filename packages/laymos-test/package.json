{
  "name": "laymos-test",
  "version": "0.0.0",
  "private": true,
  "description": "Consumer-level integration tests for Laymos test tooling",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "fmt": "vp fmt",
    "lint": "vp check && tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "effect": "catalog:",
    "laymos": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:typescript6",
    "vitest": "catalog:"
  }
}
