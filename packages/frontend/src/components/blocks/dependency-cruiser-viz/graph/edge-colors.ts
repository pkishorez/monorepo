/** Slate — an allowed downward import. */
export const LEGAL_EDGE_COLOR = '#94a3b8';
/** Red — an import that breaches the layer ordering. */
export const BREACH_EDGE_COLOR = '#ef4444';
/** Sky — an edge from a module that consumes the focused module. */
export const INCOMING_EDGE_COLOR = '#38bdf8';
/** Amber — an edge to a module the focused module consumes. */
export const OUTGOING_EDGE_COLOR = '#f59e0b';
