{
  "name": "dynamodb-client",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*"
  },
  "scripts": {
    "dev": "tsc --watch --project build.tsconfig.json",
    "build": "pnpm generate && echo 'generating types...\n' && tsc --project build.tsconfig.json && echo '✅ done'",
    "generate": "bun scripts/generate.ts",
    "lint": "pnpm lint:tsc && pnpm lint:eslint",
    "lint:tsc": "tsc --noEmit",
    "lint:eslint": "eslint .",
    "test": "vitest --reporter=verbose",
    "test:run": "vitest run",
    "test:watch": "vitest --watch --reporter=verbose"
  },
  "peerDependencies": {
    "effect": ">3"
  },
  "dependencies": {
    "@aws-sdk/credential-providers": "3.893.0",
    "aws4fetch": "1.0.20"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "3.893.0",
    "@effect/platform": "0.90.10",
    "@effect/platform-node": "0.96.1",
    "@tsconfig/node22": "22.0.2",
    "@types/node": "24.5.2",
    "effect": "3.17.14",
    "eslint": "9.36.0",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
