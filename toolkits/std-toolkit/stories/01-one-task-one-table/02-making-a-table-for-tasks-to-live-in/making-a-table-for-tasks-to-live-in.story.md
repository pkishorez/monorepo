The shape of a task exists, but there is nowhere to put one. Before the first task can be saved, the board needs a table. This chapter makes the smallest table that works, and every later chapter stores its rows in it.

The table is built from a name and two key attributes, nothing more. It does not know what a task is; deciding how a task fits into it is the next chapter's job.
