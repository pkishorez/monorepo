DynamoDB is the one database in these chapters whose tables are usually created by infrastructure code rather than by the app, so it is the one that has to describe its own shape as a create-table input. It has no native reads or writes: every read and write goes through the same table as the other databases, so your code never handles DynamoDB's own attribute format. The story here runs without a database at all.
