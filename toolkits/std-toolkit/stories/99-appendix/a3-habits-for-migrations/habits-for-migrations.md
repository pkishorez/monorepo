A shape that changes after it has shipped is safe as long as a few habits hold: put a new version on the end instead of editing an old one, make every step accept every value the previous version allowed, and keep a step from looking anywhere but at the row in front of it. These stories show each habit with a task shape, plus the one helper for partial updates and what it does not check.
