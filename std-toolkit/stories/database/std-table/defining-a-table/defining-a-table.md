# Defining a table

A name, a partition key, and a sort key. That is a complete table.

Everything else — local and global secondary indexes — is optional, and exists
so the same rows can be read a second and third way. The Story here covers the
minimum and what an index adds to it.

The shapes a table _refuses_ to build are in **Reference → Table limits &
failures**.
