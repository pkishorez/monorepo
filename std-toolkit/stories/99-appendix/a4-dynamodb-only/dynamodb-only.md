DynamoDB is the one database in these chapters that lives on the other side of a network and keeps several copies of your data, and a few features exist only because of that: batch writes, in-place arithmetic, consistent reads, a create-table input for infrastructure code, and the raw client underneath it all. These stories run each one against a real local DynamoDB and say what each costs in portability.
