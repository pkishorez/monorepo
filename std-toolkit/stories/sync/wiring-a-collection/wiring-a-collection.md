# Wiring a collection

The first note to make the round trip.

One entity, one collection, one mounted live query — and then the four things
that can happen to a note: the backend creates it, the browser creates it, the
browser changes it, the browser removes it.

Everything later in this part is a complication of this one picture.
