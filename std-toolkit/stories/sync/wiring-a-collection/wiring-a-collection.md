# Wiring a collection

The first note that makes the complete trip.

There is one entity, one collection, and one live query. Four things can then
occur to a note. The backend creates it. The browser creates it. The browser
changes it. The browser removes it.

Each Story later in this part adds to this one picture.
