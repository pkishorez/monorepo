# Catching up

A browser that has been away.

The backend keeps its history whether or not a browser watches it. When a tab
opens after a week, mounting the query starts the worker. The worker then reads
the pages that the tab missed.

The second Story is the more interesting one. There can be old work behind you
and new edits arriving at the same time. The gap then closes from both ends.
