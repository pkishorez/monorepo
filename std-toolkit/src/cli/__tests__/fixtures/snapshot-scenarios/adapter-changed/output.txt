╭─ DATABASE CONTRACT CHANGED ────────────────────╮
│ 1 unverifiable                                 │
╰────────────────────────────────────────────────╯

┌────────────────┬─────────────────────────────────────────────────┬──────────┬────────┐
│ Status         │ Change                                          │ Before   │ After  │
├────────────────┼─────────────────────────────────────────────────┼──────────┼────────┤
│ ? UNVERIFIABLE │ Cannot compare dynamodb table with sqlite table │ dynamodb │ sqlite │
└────────────────┴─────────────────────────────────────────────────┴──────────┴────────┘

UNVERIFIABLE

  Cannot compare dynamodb table with sqlite table
  /adapter

  ┌────────┬──────────┐
  │        │ Value    │
  ├────────┼──────────┤
  │ Before │ dynamodb │
  │ After  │ sqlite   │
  └────────┴──────────┘

╭─ DATABASE CONTRACT ────────────────────────────╮
│ Table: sqlite                                  │
╰────────────────────────────────────────────────╯

PRIMARY INDEX

┌───────────────┬───────────┐
│ Key           │ Attribute │
├───────────────┼───────────┤
│ Partition key │ pk        │
│ Sort key      │ sk        │
└───────────────┴───────────┘

SECONDARY INDEXES

None

ENTITIES

┌──────┬───────┬────────┬──────────┬────────────┬────────────┐
│ Name │ Kind  │ Schema │ Identity │ Primary PK │ Primary SK │
├──────┼───────┼────────┼──────────┼────────────┼────────────┤
│ User │ keyed │ User   │ id       │ email      │ id         │
└──────┴───────┴────────┴──────────┴────────────┴────────────┘

SCHEMAS

User · entity · identity: id
└─ v1
   ├─ encoded
   │  ├─ _v: "v1"
   │  ├─ email: string
   │  ├─ id: string
   │  └─ profile: { address: { city: string; country: string }; displayName: string; tags: string[] }
   └─ decoded
      ├─ email: string
      ├─ id: string
      └─ profile: { address: { city: string; country: string }; displayName: string; tags: string[] }

Snapshot was not updated.

Review the changes, then approve them with:

  std-toolkit snapshot -u
