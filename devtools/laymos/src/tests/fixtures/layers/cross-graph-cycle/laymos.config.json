{
  "sourceRoots": ["src"],
  "layers": {
    "app": { "paths": ["src/app"] },
    "domain": { "paths": ["src/domain"] }
  },
  "layerGraphs": {
    "architecture": {
      "rules": { "app": ["domain"] }
    },
    "reverse": {
      "rules": { "domain": ["app"] }
    }
  }
}
