/** Public API and CLI consumers use this to query source dependencies. */
export { deps } from './deps.js';
/** CLI error rendering uses this to identify an invalid dependency target. */
export { DepsTargetError } from './deps.js';
