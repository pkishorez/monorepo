// Architecture capabilities use this to load one Config and FileGraph.
export { loadProject } from './load-project.js';
// Internal capability tests name the loaded project boundary they replace.
export type { LoadedProject } from './load-project.js';
