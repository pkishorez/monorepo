---
title: Offline sync and storage
description: How offline sync works — the three storage layers, the storage backends, the partition lifecycle, and the key invariants.
---

## How offline sync works

Offline sync is not a bolt-on cache: when configured, Offline Storage is the **live backend** for the engine's durable layers. Server truth and sync cursors are written to it as they change, so closing the tab loses nothing — the next mount projects the stored truth into the collection immediately (before any network call), and each strategy resumes from its persisted cursor instead of re-syncing from scratch.

## Three storage layers

Three storage layers exist per collection. They are distinct and must not be conflated.

```
Server entities
  │
  ▼
Source of Truth (per collection)
  - server-confirmed entities only, one entity namespace per collection
  - atomic batch writes; convergence and tombstones applied here
  - optionally backed by Offline Storage group sot/{schema.name}
  - live even when the TanStack collection is unmounted
  │
  ▼
Collection projection (ephemeral)
  - TanStack DB rows visible to the UI; rebuilt from SoT on mount
  - may contain rows from inactive partitions

Sync State (per collection + partition)
  - serializable strategy-owned data (e.g. a cursor)
  - decoded through the strategy state schema before the strategy sees it
  - optionally backed by Offline Storage group state/{schema.name}
  - never advanced by registry, mutation results, or manual persisted writes
```

Persisted writes made while a collection is unmounted remain the state that later mounts and strategies observe. **On mount:** the engine projects current SoT into the collection before the strategy lifecycle starts. **On page reload:** the projection clears; SoT and Sync State survive only when the configured storage backend is durable.

## Storage backends

**IndexedDB** — the durable browser backend, for real offline persistence:

```ts
import { idbStorage } from 'std-toolkit/tanstack-sync/offline-storage/idb';

const std = createStdSync({
  offlineStorage: idbStorage({ name: 'app-sync' }),
});
```

`idbStorage` is safe to construct during SSR/module evaluation — it only touches `indexedDB` when a storage operation actually runs in the browser. Bump `version` when you need a clean break from previously stored data.

**Memory** — when no durable storage is configured, the engine falls back to an in-memory storage with the same interface. Everything behaves identically within a session (unmounted writes, resume-on-remount), but a page reload starts fresh. This is the default; you don't import it.

**Inheritance and opt-out** — root storage passed to `createStdSync` is inherited by every collection that omits `offlineStorage`; a collection overrides with its own storage, or opts out entirely with `offlineStorage: false`.

## Data flow on strategy sync

1. Strategy reads its own decoded Sync State (`ctx.getState`).
2. Strategy fetches or subscribes.
3. Strategy calls `ctx.writeServerTruth(entities)`.
4. Engine atomically writes the batch to SoT and projects accepted state if mounted.
5. Strategy advances its own Sync State (`ctx.setState`) — SoT first, then state.

Stale-but-valid entities are successful no-ops. On `writeServerTruth` failure the strategy lets the `WriteError` surface; the engine logs it and restarts `run` on a 2-second spaced schedule. If persisted Sync State belongs to a different strategy name or fails the state schema, the engine warns, resets that partition to the strategy's empty state, and continues.

## Partition lifecycle

`sync` uses TanStack DB's on-demand subset lifecycle for its `partitions` map:

```
loadSubset(options)
  -> find a partitions-map field eq partitionValue; derive partitionKey
  -> increment that partition's refcount
  -> 0 -> 1: start the partition's strategy runtime
  -> no match: covered by the total sync entry if present, else console.error

unloadSubset(options)
  -> decrement refcount
  -> 1 -> 0: stop that partition's strategy runtime (keep its Sync State for resume)

collection cleanup
  -> stop the global runtime and every active partition runtime
  -> keep SoT and Sync State in the engine closure
```

The total sync entry runs under the reserved `__total__` partition key, started unconditionally at sync start. `singleItemSync` uses collection-level lifecycle only.

## Key invariants

1. **One instance, one tracker, one registry.** Collections from different instances are invisible to each other's registries.
2. **Entity ownership is disjoint.** An instance may register a `schema.name` only once.
3. **Entity identity is `schema.name`.** Every accepted envelope must have `meta._e === schema.name`; mismatches are rejected, not routed.
4. **Total and per-partition sync entries coexist** in one collection, one engine, one Source of Truth.
5. **The engine owns SoT storage; strategies own Sync State meaning.**
6. **Sync State is advanced only by strategy sync.** Registry writes, mutation results, and manual persisted writes never advance it.
7. **Server-truth writes are atomic.** A batch succeeds whole or fails through `WriteError`.
8. **Stale valid envelopes are successful no-ops.**
9. **Tombstones stay in SoT.** Projection removes the row; SoT keeps the winning `_d: true`.
10. **SoT can be written while unmounted.** Projection is deferred until callbacks exist.
11. **`persist` is always explicit** on public writes and registry messages.
12. **Two strategy families, no crossover** — partitioned vs. single-item.
13. **Users do not author Entity Meta for normal mutations.** Handlers return server envelopes.
14. **The public surface is a handful of values, zero exported types.** All config/result types flow through inference.
